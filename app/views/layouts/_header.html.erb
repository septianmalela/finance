	<%= link_to "EXPORT AS PDF", tabungan_path(@tabungan, format: :pdf), class: 'btn btn-primary', style: 'font-size: 20px; ' %>
	<%= link_to "EXPORT CSV", tabungan_path(format: "csv"), class: 'btn btn-primary', style: 'font-size: 20px; ' %>